# --- Core (local tracker + shared) ---
Flask>=3.0.0
psutil>=5.9.0

# --- Server (admin/user dashboards, multi-user) ---
Flask-SQLAlchemy>=3.1.0
Flask-Login>=0.6.3
SQLAlchemy>=2.0.0

# --- Agent (screenshots) ---
mss>=9.0.0
Pillow>=10.0.0

# --- Platform-specific (installed automatically only where applicable) ---
pywin32>=306 ; platform_system == "Windows"
pyobjc-framework-Quartz>=10.0 ; platform_system == "Darwin"
